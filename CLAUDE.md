# 釣り・ニュース・時計ダッシュボードアプリ — Claude Code 作業ガイド

Androidタブレットを卓上キオスクとして常時設置し、大型時計・釣り場の潮汐/天気・ゲーム/AI/ITニュースを
自動更新表示するダッシュボードアプリ。Flutter / Dart。詳細仕様は `docs/2026-09-23-仕様書.pdf`。

## このプロジェクトについて

このリポジトリ（フォルダ名 `javis`）は本アプリ専用。他プロジェクト（`gocco-race` 等）とは無関係。
Flutterプロジェクトはリポジトリ直下に作成する（`wall_jarvis` という1アプリのみを扱う。
applicationId は `com.nadaryu.wall_jarvis`）。

## バージョン管理
- 使用ツール: Git。リモートは未設定（ローカルのみ）。
- 既定ブランチは `main`。個人開発のため機能ブランチは必須ではないが、大きめの変更は分けてよい。
- 無視対象: `build/`, `.dart_tool/`, `android/.gradle/`, `ios/Pods/` など（`.gitignore` に定義）。
- **`docs/2026-09-23-仕様書.pdf` を動かさない・上書きしない。** 元の仕様書。要約や更新は別ファイル
  （`docs/` 配下に日付付きで新規作成）に書き、原本は残す。

## 版管理の運用（AIエージェント向け手順）
- 版管理の操作はすべて `git` コマンドで行う。
- 手順は `git status` → 変更 → `git diff` で確認 → コミット。
- **ユーザーから明示的な指示があるまで、コミットしないこと。** 作業が終わったら「コミットしてよいか」を
  日本語で確認する。
- コミットメッセージは日本語で書く。何を変更したかだけでなく、なぜそうしたかを残す。
- **コミット前に必ず `git status` で変更一覧を確認**し、`build/` などの生成物が混入していないことを
  確かめる。混入していたら `.gitignore` の不備を疑う。
- 作業前に `git status` で最新状態を確認する（リモートなしのため pull は不要）。

## マルチセッション運用ルール
- 個人開発の単独プロジェクトのため、基本は単一セッションで作業する。
- 複数セッションを並行させる場合は、作業ツリーを `git worktree add` で分離すること。

## 開発環境の起動手順
- Flutter SDK: `C:\src\flutter`（git clone, stable channel, `--depth 1`）。PATHに `C:\src\flutter\bin` を追加済み。
- Android SDK: `%LOCALAPPDATA%\Android\sdk`。環境変数 `ANDROID_HOME` / `ANDROID_SDK_ROOT` に設定済み。
- JDK: Temurin 17（winget導入）。
- 実行確認: `flutter doctor -v` を通し、すべて✓になっていることを確認してから作業を始める。
- 実機（Androidタブレット）を優先動作確認先とする。エミュレータを使う場合は
  `flutter emulators --launch <id>` → `flutter run` の順。
- **Windows側の書き換えはbashマウント経由だと最新に見えないことがある。** 疑わしければPowerShellで確認する
  （姉妹プロジェクトgocco-raceで実測済みの地雷）。

## 外部ツール連携（MCP 等）
- 現時点でプロジェクト固有のMCPサーバ接続はなし。Android実機操作はADB経由（`flutter run`任せ）で足りる想定。
  将来デバイス操作の自動化が必要になったら、この節に追記する。

## 実装方針
**Flutterの標準的な構成で実装する方針。** 状態管理はRiverpod。バックグラウンド定期更新は
`flutter_workmanager`、画面常時点灯は`wakelock_plus`、ローカルキャッシュは`Hive`（または`sqflite`）を使う
（仕様書「技術スタック・アーキテクチャ」節のとおり）。

キオスク運用（Android Lock Task Mode、スリープ無効化）はAndroidネイティブ設定が絡むため、
`android/app/src/main/` 配下を直接編集する場面がある。変更時は理由を1行コメントで残す。

## 検証
- 完了の判定に使うチェック: `flutter analyze` と `flutter test`。
- UI変更は実機またはエミュレータで実際に動かして確認する（テストだけで完了と報告しない）。
- **完了報告には、実行したコマンドとその出力を根拠として添えること。** 主張だけで完了にしない。

## ガードレール
### 変更してはいけないもの
- `docs/2026-09-23-仕様書.pdf`（原本、上書き禁止）
- APIキー・認証情報（`.env`等に分離し、コミットしない）
- `android/key.properties` やキーストア（署名鍵、`.gitignore`済みだが誤って生成・コミットしないよう注意）

### 止まる条件
次のどれかに当てはまったら、作業を中断してユーザーに報告すること。
- 同じ修正を3回試しても検証（`flutter analyze` / `flutter test` / 実機確認）が通らない
- 2回連続で検証結果が改善しない
- 外部APIの利用規約・商用利用可否が確認できないまま実装を進めることになりそうなとき
  （仕様書「未確定事項」参照。データソース選定はユーザー確認が必要）
- 上の「変更してはいけないもの」に手を入れないと先に進めない

## AIエージェントへのルール (STRICT CONSTRAINTS)
1. **出力の完全日本語化**: 分析、提案、実装計画、タスクリスト、版管理のコミットメッセージ等、AIが行う
   すべての出力・報告は必ず日本語で行うこと。
2. **ステップバイステップの合意形成**: "Plan, Confirm, Execute"（計画し、確認し、実行する）のプロセスを
   遵守し、勝手に実装を進めないこと。
3. **ユーザーの承認待機**: 実装や修正を開始する前に「この方針で進めてよいか」を必ず日本語で問いかけ、
   ユーザーから明確な承認を得るまでコードの生成・書き換えを行わないこと。ただし、**変更内容を1文で
   説明できる小さな修正**（誤字、ログ追加、変数名の変更など）は確認を挟まずに実施してよい。
4. **LEARNINGS.md の参照と手入れ**: 各セッション開始時に、1. LEARNINGS.md を読みます。2. 読んだ内容を
   簡潔に要約し、処理済みだとわかるようにしてください。3. 作業の終わりに追記したら、その場で
   `wc -c LEARNINGS.md` を確認し、40KBを超えていたら切り出しを提案すること。
5. **本ファイルの優先**: 本ファイルと過去の設計書・引き継ぎ文書が食い違った場合、常に本ファイルが正。
   文書側が古い旨を指摘したうえで本ファイルに従うこと。
